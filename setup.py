from setuptools import setup

setup(name='imagespawner-videoamp',
      version='0.1',
      description='Docker-based spawners with image selection',
      url='https://github.com/VideoAmp/imagespawner',
      packages=['imagespawner'],
      zip_safe=False)
